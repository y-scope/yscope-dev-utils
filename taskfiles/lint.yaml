version: "3"

includes:
  venv:
    flatten: true
    taskfile: "./lint-venv.yaml"
  yaml:
    flatten: true
    taskfile: "./lint-yaml.yaml"

vars:
  G_LINT_VENV_DIR: "{{.G_OUTPUT_DIR}}/lint-venv"
  G_LINT_VENV_CHECKSUM_FILE: "{{.G_OUTPUT_DIR}}/lint#venv.md5"

tasks:
  check:
    desc: "Runs all linters to identify any violations."
    cmds:
      - task: "yaml-check"
