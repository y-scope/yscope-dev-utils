version: "3"

includes:
  boost: "boost/tests.yaml"
  remote: "remote/tests.yaml"
  scripts: "scripts/tests.yaml"

tasks:
  all:
    internal: true
    cmds:
      - task: "boost"
      - task: "remote"
      - task: "scripts"

  boost:
    cmds:
      - task: "boost:test"

  remote:
    cmds:
      - task: "remote:default"

  scripts:
    cmds:
      - task: "scripts:default"
