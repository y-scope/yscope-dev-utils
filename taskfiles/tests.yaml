version: "3"

includes:
  boost: "boost/tests.yaml"
  checksum: "checksum/tests.yaml"
  docker: "docker/tests.yaml"
  remote: "remote/tests.yaml"
  ystdlib-py: "ystdlib-py/tests.yaml"

tasks:
  all:
    internal: true
    cmds:
      - task: "boost"
      - task: "checksum"
      - task: "docker"
      - task: "remote"
      - task: "ystdlib-py"

  boost:
    cmds:
      - task: "boost:test"

  checksum:
    cmds:
      - task: "checksum:default"

  docker:
    cmds:
      - task: "docker:default"

  remote:
    cmds:
      - task: "remote:default"

  ystdlib-py:
    cmds:
      - task: "ystdlib-py:default"
